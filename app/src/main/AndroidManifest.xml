<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.bob.bobapp">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.CHANGE_WIFI_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">

        <activity android:name=".activities.RiskProfileActivity"/>

        <activity android:name=".activities.TermsAndConditionActivity"/>

        <service android:name=".api.WebService" android:exported="false" />

        <activity android:name=".activities.PortfolioAnalytics" />

        <activity android:name=".activities.WealthMgmtActivity" />

        <activity android:name=".activities.InvestmentCartActivity" />

        <activity android:name=".activities.InvestmentMaturityActivity" />

        <activity android:name=".activities.SIPSWPSTPDueActivity" />

        <activity android:name=".activities.StopSIPActivity" />

        <activity android:name=".activities.DematHoldingActivity" />

        <activity android:name=".activities.OrderStatusActivity" />

        <activity android:name=".activities.DiscoverFundsActivity" />

        <activity android:name=".activities.HoldingsActivity" />

        <activity android:name=".activities.TransactionActivity" />

        <activity android:name=".activities.RealizedGainLossActivity" />

        <activity android:name=".activities.InsuranceActivity" />

        <activity android:name=".activities.FactSheetActivity" />

        <activity android:name=".activities.HoldingDetailActivity" />

        <activity android:name=".activities.BuySIPRedeemSwitchActivity" />

        <activity android:name=".activities.InsuranceDetailActivity" />

        <activity android:name=".activities.BOBActivity">
            <!--<intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>-->
        </activity>

    </application>

</manifest>